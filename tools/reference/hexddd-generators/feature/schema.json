{
  "$schema": "http://json-schema.org/schema",
  "$id": "feature",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the feature",
      "x-prompt": "What is the name of the feature?",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "domain": {
      "type": "string",
      "description": "The name of the domain to create the feature in.",
      "x-prompt": "Which domain does this feature belong to?"
    },
    "directory": {
      "type": "string",
      "description": "A directory where the feature will be created."
    }
  },
  "required": [
    "name",
    "domain"
  ],
  "additionalProperties": false
}
