# Environment configuration for generated projects
#
# Duplicate this file per environment (`.env.local`, `.env.staging`, etc.)
# and populate values before committing. Keep secrets encrypted (see SOPS docs).

VIBES_ENV=development

#
# Security hardening
#
# When security hardening is enabled, generate a 64-character hex key:
#   openssl rand -hex 32
# ENCRYPTION_KEY=

#
# Logfire configuration (DEV-PRD-018 / DEV-SDS-018)
#
LOGFIRE_TOKEN=
LOGFIRE_ENVIRONMENT=development
LOGFIRE_SERVICE_NAME=app-api
LOGFIRE_SEND_TO_LOGFIRE=if-token-present
# LOGFIRE_SERVICE_VERSION=

#
# OpenTelemetry defaults
#
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf
OTEL_SERVICE_NAME=app-api
