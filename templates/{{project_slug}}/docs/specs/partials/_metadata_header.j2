```jinja
{# Project-specific metadata header for generated docs. Falls back to root partial values. #}
{% set repo_url_value = repo_url if repo_url else (root_repo_url if root_repo_url is defined else 'TBD') %}
{% set year_value = year if year else (root_year if root_year is defined else 'TBD') %}

**Project:** {{ project_name }} ({{ project_slug }})
**Maintainer:** {{ author_name }}
**Repository:** {{ repo_url_value }}
**Year:** {{ year_value }}

---

{# Render a consistent metadata header for Copier-generated documentation.
   Uses Copier variables directly with sensible fallback values. #}
{# Keep a single canonical implementation to avoid duplicate content in generated docs. #}

```
