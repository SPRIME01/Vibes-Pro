# example; will be encrypted by sops
APP_ENV=dev
APP_VERSION=0.1.0

# External API Keys
#OPENAI_API_KEY=sk-...
SUPABASE_URL=...
SUPABASE_ANON_KEY=...

# OpenObserve Configuration (Phase 4: Storage & Analytics)
# Base URL for OpenObserve instance (e.g., https://observe.vibepro.dev:443 or http://localhost:5080)
OPENOBSERVE_URL=https://observe.vibepro.dev:443
# API token for authentication (Basic Auth password)
OPENOBSERVE_TOKEN=...
# Organization name (defaults to 'default' if not specified)
OPENOBSERVE_ORG=default
# User email for Basic Auth (defaults to 'root@example.com' if not specified)
OPENOBSERVE_USER=sprime01@gmail.com

# OTLP Configuration
# Endpoint for OTLP gRPC export (used by vibepro-observe crate)
OTLP_ENDPOINT=http://127.0.0.1:4317
