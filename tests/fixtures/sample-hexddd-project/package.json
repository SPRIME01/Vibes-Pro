{
    "name": "sample-hexddd-project",
    "version": "1.0.0",
    "description": "Sample HexDDD project for migration testing",
    "scripts": {
        "build": "nx build",
        "test": "nx test",
        "generate": "nx generate"
    },
    "devDependencies": {
        "@nrwl/workspace": "^15.0.0",
        "@types/node": "^18.0.0"
    }
}
