// Let's create a simple test to understand what's available in tsink
use tsink;

fn main() {
    println!("tsink version: checking available types");
    // This will help us see what's actually available
}
