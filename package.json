{
    "name": "vibes-pro",
    "version": "0.1.0",
    "description": "Unified generator-first platform combining HexDDD and VibePDK",
    "type": "module",
    "scripts": {
        "build": "nx run-many --target=build --all",
        "test": "nx run-many --target=test --all",
        "test:jest": "jest --config jest.config.json",
        "test:jest:watch": "jest --config jest.config.json --watch",
        "test:jest:coverage": "jest --config jest.config.json --coverage",
        "test:integration": "jest --config jest.config.json --testPathPattern=tests/integration",
        "test:integration:watch": "jest --config jest.config.json --testPathPattern=tests/integration --watch",
        "test:e2e": "jest --config jest.config.json tests/integration/end-to-end.test.ts --timeout=300000",
        "lint": "nx run-many --target=lint --all",
        "dev": "nx run-many --target=serve --all --parallel=5",
        "generate": "copier copy . ../test-output",
        "test-generation": "copier copy . ../test-output && cd ../test-output && pnpm install && pnpm build"
    },
    "workspaces": [
        "templates/*",
        "tools/*"
    ],
    "devDependencies": {
        "@nx/jest": "^21.5.3",
        "@nx/js": "19.8.4",
        "@nx/node": "19.8.4",
        "@nx/workspace": "19.8.4",
        "@types/jest": "^30.0.0",
        "@types/js-yaml": "^4.0.9",
        "@types/node": "^24.5.2",
        "@typescript-eslint/eslint-plugin": "^7.18.0",
        "@typescript-eslint/parser": "^7.18.0",
        "eslint": "^8.57.1",
        "jest": "^30.1.3",
        "jest-extended": "^6.0.0",
        "js-yaml": "^4.1.0",
        "nx": "19.8.4",
        "rimraf": "^6.0.1",
        "ts-jest": "^29.4.4",
        "ts-morph": "^27.0.0",
        "typescript": "^5.9.2",
        "vitest": "^1.6.1"
    },
    "engines": {
        "node": ">=18.0.0",
        "pnpm": ">=8.0.0"
    },
    "overrides": {
        "vite-node": "1.6.1"
    },
    "packageManager": "pnpm@9.0.0"
}
